#!/usr/bin/env ruby

require "rubygems"
require "filewatch/tail"
require "optparse"

tail = FileWatch::Tail.new
ARGV.each { |f| tail.watch(f) }
tail.subscribe do |path, line|
  puts "#{path}: #{line}"
end
